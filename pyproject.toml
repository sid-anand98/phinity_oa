[project]
name = "apb-gpio-ctrl"
version = "0.1.0"
description = "APB3 GPIO Controller with Secret Output Toggle - Verilog RTL implementation and test suite"
requires-python = ">=3.10"
dependencies = [
    "cocotb>=2.0.0",
    "pytest>=9.0.1",
]